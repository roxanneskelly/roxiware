<div class="large_form" id="page_edit_form">
  <%= form_for @page, :method=>:put, :url=>page_path(@page.page_type) do |page| %> 
      <%= page.text_area :content, :class=>"settings_wysiwyg" %>
      <%= button_tag "Save", :id=>"save", :value=>"save", :type=>"submit", :class=>"save_button" %></div>
  <% end %>
</div>
<script>
$(document).bind("ready", function() {
});
</script>

